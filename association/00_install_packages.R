# R packages required by the numbered association analysis scripts
pkgs <- c(
  "tidyverse",
  "arrow",
  "data.table",
  "lubridate",
  "tsModel",
  "future",
  "future.apply",
  "parallelly"
)
install.packages(pkgs)
